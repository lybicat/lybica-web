import React from 'react';
import { Flex } from 'react-layout-pane'

export default React.createClass({
  render() {
    return (
      <Flex style={{}}>
        <h1>Dashboard</h1>
        This is a dashboard!
      </Flex>
    );
  }
});

