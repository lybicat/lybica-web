import React from 'react';

const styles = {
  textAlign: 'right',
  paddingRight: '1em'
}

export default React.createClass({
  render() {
    return (
      <div style={styles}>
        Zhang Yu
      </div>
    );
  }
});

